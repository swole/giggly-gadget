# Giggly Gadget - feature map (how to reach and verify every feature)

AGENTS.md says how the code works. This file says how an agent *drives* the app and what each page must show, so a report like "the pills aren't showing on the plan page" can be reproduced without Johnny checking on his phone. Keep it current: when a page, tab, chip or role rule changes, change its row here in the same commit (the tests gate the deploy; this file gates the verification).

## Drive it
- **Dev server:** Browser pane -> `preview_start {name: "giggly-gadget"}` (launch config in `9. Claude/.claude/launch.json`, port 3010, autoPort), or `npm run dev` in `C:/Users/johnn/code/giggly-gadget`.
- **Dev = production data.** `.env.local` (from `vercel env pull`) points at the prod Supabase and Notion. Never fill, randomize or clear the *current* or *next* week while verifying. Use a **sandbox week**: any Monday at least 8 weeks out, `/plan?week=YYYY-MM-DD`. Clear it when done (Week actions -> Clear the week, or `POST /api/plan/clear {week_of}`).
- **Phone width first:** `resize_window {width: 390, height: 844}`. Every page is designed at 390; desktop width hides nothing but is not what the household sees.
- **Role, no login.** First load shows the RoleSheet ("Who is using this phone?") with Johnny / Lydia / Shallaine. Set it directly from `javascript_tool`: `await fetch('/api/role', {method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({role:'johnny'})})` then reload. Roles: `johnny | lydia | helper`. `?as=helper` on any URL picks helper for a cookie-less browser. Switch via the tab-bar button (aria-label "Signed in as <Name>. Switch person."). `DELETE /api/role` clears.
- **Tab bar** (nav aria-label "Primary"): planners see Kitchen · Plan · Grocery · Recipes · Add; helper sees Kitchen · Grocery · Recipes. No tab bar in cook mode or before a role is picked.
- **Read, don't screenshot:** `read_page` / `find` on the labels below; screenshot only as proof at the end. `read_console_messages` after every page load.
- **Realtime:** plan and grocery changes propagate between tabs (Supabase Realtime). A second Browser-pane tab is how "shows up on the other phone" is verified.

## Pages - what must be true

| Route | Roles | Reach it | Must show / key controls | Verify by |
|---|---|---|---|---|
| `/` Kitchen (`/kitchen` redirects) | all | tab Kitchen | Today's meal cards: "Cook", "Recipe", "Mark cooked" (8 s Undo; aria "Keep as cooked"). Tomorrow card with "Tonight" prep hints inside it. Rest of the week. Empty today: Saturday shows next week's list with counts, other days name the next planned day. "Pack Johnny's lunch for the office" when lunch = office today/tomorrow. | Kitchen is calendar-driven (today), so a sandbox week never appears here. Verify read-only against the real week. **Mark cooked writes cook_log + last_made**: never as a test unless you Undo within 8 s. |
| `/plan?week=` | planners; helper read-only | tab Plan | Mon-Sat grid, Sun collapsed. Header **Randomize** die (hidden on a blank week; the hero CTA covers it), a die on each day card and on every empty slot -> RandomizeSheet (theme chips: added by / heart healthy / cuisine / <= 30 min / want to try / 4+ stars / 5-star only; live match counts; "Roll those again"). Slot tap -> picker (search box; chips Heart healthy / Want to try / 4+ / Recent; "Filters" for cuisine and Lydia's picks; Leftovers tab; type-to-create one-off `Add "..." as a one-off`; "Pairs with {main}" chip when the slot holds a main). Chip menu: note / Pick another / open / remove (Undo). Tap J+L to cycle eaters. **Lunch pills** on every lunch row: `J · home` / `L · office`. Constraint chips: sage tick / "2 more oily fish" / terra "Chicken x3 - cap is 1". Per-day protein J/L. **Week actions** sheet: fill from rotation, copy last week, Clear the week (cooked kept), Print. **Share** (WhatsApp digest + `?as=helper` link). From Friday the default week pivots forward with an escape link. | Sandbox week: Randomize -> all three constraint chips resolve -> Pick another swaps one -> lunch pill toggles home/office -> Clear the week empties it. `/grocery?week=` for that week auto-rebuilt. |
| `/plan/print?week=` | planners | Week actions -> Print | One A4: week table + shopping list by shop; `pack lunch: Johnny` when applicable. | Open for the sandbox week after a roll. |
| `/grocery?week=` | all | tab Grocery | By shop (wet market / supermarket / either) or by aisle; progress bar; all-bought state; pantry staples hidden (planners edit the list); free-text add (aria "Add an item"); week nav (aria "Week", "Previous week", "Next week"); planners: Rebuild from plan / clear. From Friday: banner to next week's list; a future week shows "Shopping ahead". | Sandbox week after a roll: items grouped; tick one -> survives reload and shows ticked in a second tab. |
| `/recipes` Discover | all | tab Recipes | Paginated cards; density toggle (aria "View density"); Filters sheet with counts (Pinned to try / <= 30 min / Heart healthy / Lydia's picks); search. | Search "salmon" -> cards. Helper sees no rating or want-to-try controls. |
| `/recipes/[id]` | all | card tap | Hero (video recipes: play badge). Scale mode Both / Johnny / Lydia + "By servings"; ingredient amounts rescale. "Start cooking" (solid terra). "Plan this" sheet -> day / slot / eaters. "Learn how to make it" card after the Method. Rating stars (aria "Rating (1–5, half-star steps)") and want-to-try pin: planners only, **both write to Notion**. | Scale Johnny -> protein and grain lines x0.6, others x0.5. Do not rate or pin as a test. |
| `/recipes/[id]/cook` | all | Start cooking, or Kitchen "Cook" | Full screen, no tab bar. "Step N of M"; left third of the step area pages back, right two-thirds forward; amounts inline in steps; Ingredients peek sheet from the footer; timers (aria "Dismiss timer"). "Cooked it" marks the planned meal. | Step index persists per recipe+meal (sessionStorage). "Cooked it" writes cook_log: sandbox week only. |
| `/add` | planners | tab Add | Link (aria "Recipe link", "Paste link from clipboard", "Clear link"), text (aria "Recipe text") or photo -> draft (aria "Title", "Emoji", "Remove line", "Remove step", "Notes") -> create in Notion -> sync -> recipe page with the Plan-this nudge (`?new=1`). `/add?url=` auto-runs. | **Spends money (Claude) and writes Notion.** Verify the extract step with a real URL only when the change touches it; never leave test recipes in prod Notion (delete the page, re-sync). |

## API surface for verification (planner cookie where marked)
`GET /api/plan/meals?week=YYYY-MM-DD` -> `{week_of, meals, lunch}` · `POST /api/plan/randomize {week_of, days?, slots?, mode: "fill_empty"|"replace", replace_ids?, include_sunday?}` (planner) · `POST /api/plan/clear {week_of}` (planner) · `PUT /api/plan/lunch {planned_for, person: "johnny"|"lydia", location: "home"|"office"}` (planner) · `GET /api/recipes/<id>/ingredients` (check `scalable`) · `GET /api/sync-now?force=0&cb=<n>` -> `{upserted, errors, lint}` · `GET /api/sync-stats`.

## Report -> reproduction (the "Benny" loop)
1. `preview_start giggly-gadget`; role via `/api/role`; 390 px; `read_console_messages`.
2. Find the row above; go to the route; `find` the control by its label or aria-label.
3. If it is missing: readiness probes first (`lunchLocationsReady` -> does `lunch_locations` exist in Supabase?), then the console, then the network (`read_network_requests`).
4. Report: reproduces on dev yes/no · already fixed on main yes/no · exact page and label looked for · one screenshot as proof.

## Known non-features (don't chase)
- Kitchen only shows this week; planning a sandbox week never changes it.
- iOS has no share-sheet path into the PWA; the Paste button is the iOS route. Instagram/Facebook links cannot be read by `/add` (paste the caption).
- Recent one-off chips left the picker 2026-08-30; the <= 30 min chip left it 2026-09-05 (it lives in Discover's Filters and the RandomizeSheet).
